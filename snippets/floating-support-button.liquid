{{ 'component-floating-support-button.css' | asset_url | stylesheet_tag }}

{%- comment -%}
  Visual placeholder only, per explicit instruction — no live chat app is
  installed, so this doesn't navigate or open anything yet. Swap the href
  for a real contact/help page, or replace this whole snippet's markup
  with a chat app's own embed snippet, whenever that's decided.
{%- endcomment -%}
<a
  href="#"
  class="floating-support-button"
  aria-label="{{ 'general.social.links.title' | t | default: 'Support' }}"
  role="link"
>
  <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true" focusable="false">
    <path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"></path>
  </svg>
</a>
